clear all;
load('NLOS.txt');


z_offset = 1.20;% 

x_offset = 4 * 0.8 + 0.15;

y_offset = 3 * 0.8 - 0.15;

real_dis = zeros(size(NLOS));

for i = 1:size(NLOS,1)
    for j = 1:size(NLOS,2)
        real_dis(i,j) = (((i-1) * 0.8 + y_offset)^2.0 + ...
            ((j-1) * 0.8 + x_offset)^2.0 +  z_offset^2.0)^0.5
    end
end

